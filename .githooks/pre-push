#!/usr/bin/env bash

set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"

echo "[pre-push] Running local npm ci check before push..."

cd "$repo_root"
npm ci

echo "[pre-push] npm ci passed. Continuing push."
